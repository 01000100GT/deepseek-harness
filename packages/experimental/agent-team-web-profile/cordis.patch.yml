# Private Agent Teams Web layer. Apply after dsh-web-app and the host-side
# dsh-agent-team-profile so the browser mounts only when both seams are present.

- insert:
    - id: agent-team-remotes
      name: '@deepseek-ai/dsh-agent-team-remotes'

    - id: ui-agent-team
      name: '@deepseek-ai/dsh-client-ui-agent-team'
